type Product = {
  id: string;
  images: Array<string>;
  name: string;
  price: number;
  description: string;
  color: Array<string>;
  collection: Array<string>;
  size: Array<string>;
};

export { type Product };
