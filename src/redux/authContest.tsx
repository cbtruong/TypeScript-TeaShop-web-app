
// authContest.tsx

const xApiKey = 'd3b2828447993831'; // Thay thế bằng x-api-key thực tế của bạn
const urlBackend = 'http://localhost:3000';

export const getApiKey = () => {
  return xApiKey;
};

export const getBackendUrl = () => {
  return urlBackend;
};
